---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: loxilights.netlox.netlox.io
spec:
  group: netlox.netlox.io
  names:
    kind: Loxilight
    listKind: LoxilightList
    plural: loxilights
    singular: loxilight
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Loxilight is the Schema for the loxilights API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LoxilightSpec defines the desired state of Loxilight
            properties:
              loxilightAgentConfig:
                description: LoxilightAgentConfig holds the configurations for Loxilight-agent.
                type: string
              loxilightCNIConfig:
                description: LoxilightCNIConfig holds the configuration of CNI.
                type: string
              loxilightControllerConfig:
                description: LoxilightControllerConfig holds the configurations for
                  Loxilight-controller.
                type: string
              loxilightImage:
                description: LoxilightImage is the Docker image name used by Loxilight-agent
                  and Loxilight-controller.
                type: string
              loxilightPlatform:
                description: LoxilightPlatform is the platform on which Loxilight
                  will be deployed.
                type: string
            required:
            - loxilightAgentConfig
            - loxilightCNIConfig
            - loxilightControllerConfig
            - loxilightPlatform
            type: object
          status:
            description: LoxilightStatus defines the observed state of Loxilight
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
